# Xcode Cloud Environment Variables
# This file helps Xcode Cloud locate dependencies

# Node binary path (auto-detected)
export NODE_BINARY=$(command -v node)

# Enable CocoaPods verbose logging for debugging
# export COCOAPODS_VERBOSE=1

# Force pod install on every build (For troubleshooting)
# export FORCE_POD_INSTALL=1